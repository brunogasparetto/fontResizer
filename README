jQuery fontResizer v1.0 by Eder Lima (ederlima.com.br)

Provides accessible functionalities to text.

HOW TO USE:

$('#textelementcontainer').fontResizer({
	//options
	 //number of change steps
	 incsteps : 5,
	 //number to increase or decrease the size (in pixels)
	 incsize : 2,	
	 //elements when clicked, resize the size
	 smallerbutton : $('#someMinusElement'),
	 biggerbutton : $('#somePlusElement'),
	 //an array of elements, defaults: ['div', 'p', 'li', 'blockquote', 'cite', 'dd', 'dt', 'h1','h2','h3', 'h4', 'h5', 'h6', 'span', 'small', 'td', 'th']
	 elements : ['td', 'li', 'p', 'span', ...], 
	 //some function to call on each resize, default: null
	 callback : function(){ console.log('this is a callback') } 
*/
});